import InfoBar from './InfoBar';
import Navbar from './Navbar';

const Header = () => {
    return (
        <header>
            <InfoBar />
            <Navbar />
        </header>
    );
};

export default Header;
